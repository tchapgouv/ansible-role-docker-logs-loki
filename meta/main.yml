---
galaxy_info:
  description: This ansible role is used to setup a logs stack based on Loki with docker/docker-compose on linux.
  license: "license (BSD, MIT)"
  role_name: ansible_role_docker_logs_loki
  min_ansible_version: 2.8
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
    - name: Ubuntu
      versions:
        - trusty
        - xenial
        - bionic
    - name: CentOS
      versions:
        - 7
        - 8
    - name: Amazonlinux
      versions:
        - 2
    - name: EL
      versions:
        - 6
        - 7
        - 8
  galaxy_tags:
    - logs_loki
    - docker
    - debian

dependencies:
  - docker
  - docker-compose
